# Build artifacts (see CLAUDE.md "Coding Standards" — the Dockerfile is
# generated by DockerfileModifier.sh from build_data/, never committed).
build_data/
Dockerfile.gitnexus-mcp
Dockerfile.gitnexus-mcp.*

# Session-local hook state (written by .claude/hooks at runtime).
.claude/.state/
